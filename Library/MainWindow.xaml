<Window x:Class="Library.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Library"
        mc:Ignorable="d"
        Title="АСУБ" Height="720" Width="1280" ResizeMode="NoResize" Icon="MainIcon.png" WindowStartupLocation="CenterScreen" >
    <Window.Resources>
        <local:ColorToVisibility x:Key="ColorsConverter"/>
        <local:WidthToSign x:Key="WidthConverter"/>
        <ItemsPanelTemplate x:Key="cnv" >
            <Canvas/>
        </ItemsPanelTemplate>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="30"/>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="50"/>
            <ColumnDefinition Width="{Binding Width}" MaxWidth="300" Name="col"/>
        </Grid.ColumnDefinitions>
        <GridSplitter Grid.Column="0" Grid.RowSpan="2" Width="3" IsEnabled="False"/>
        <Button Grid.Column="1" Grid.Row="1" Height="70" Content="{Binding Path=Width, ElementName=col,Converter={StaticResource WidthConverter}}" FontSize="30" Background="White" Command="{Binding Expand}"/>
        <Menu Background="White" Grid.ColumnSpan="3" Height="30" >
            <MenuItem Header="Главная" Height="30">
                <MenuItem Header="Подключение" Command="{Binding LoadData}"/>
                <MenuItem Header="Настройки"/>
            </MenuItem>
            <MenuItem Header="Администрирование" Height="30" Visibility="{Binding Path=Fill, ElementName=Color,Converter={StaticResource ColorsConverter}}">
                <MenuItem Header="Пользователи"/>
                <MenuItem Header="Добавить книгу"/>
            </MenuItem>
        </Menu>
        <StackPanel Grid.ColumnSpan="3"  HorizontalAlignment="Right" Orientation="Horizontal" >
            <Image   Source="{Binding Status}" />
            <TextBlock VerticalAlignment="Center" Text="{Binding UserName}" Padding="0,0,5,0" FontSize="15"/>
            <Button  Background="White"  BorderThickness="0" Command="{Binding Log}">
                <Image Source="C:\Users\ivang\Desktop\Library\Library\logout.png"/>
            </Button>
        </StackPanel>
        <Grid Grid.Row="1" Margin="25">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="6*"/>
                <ColumnDefinition Width="1*"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="50"/>
                <RowDefinition Height="2*"/>
            </Grid.RowDefinitions>

            <TextBox Text="{Binding SearchQuery}"  FontSize="30" />
            <Button Command="{Binding SearchPressed}" Grid.Column="1" Content="Найти" FontSize="20" BorderThickness="2" BorderBrush="Black" Background="White"/>

            <ScrollViewer CanContentScroll ="True" HorizontalScrollBarVisibility="Auto" Grid.ColumnSpan="2" Grid.Row="2" Margin="0,25,0,0">
                <Grid>
                    <!--TODO: implement auto height by property-->
                    <Viewbox  Height="8000">
                        <Canvas/>
                    </Viewbox>
                    <ItemsControl ItemsSource="{Binding Books}" ItemsPanel="{StaticResource cnv}" Grid.ColumnSpan="2" Grid.Row="2" >
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Button  Margin="{Binding Margin}" Height="400" Width="200" Background="White" BorderThickness="0" Command="{Binding SelectBook}">
                                    <StackPanel>
                                        <Image Source="{Binding Link}" Height="320" />
                                        <TextBlock Text="{Binding Author}" FontWeight="Bold" HorizontalAlignment="Center"/>
                                        <TextBlock Text="{Binding Name}" HorizontalAlignment="Center"/>
                                        <TextBlock Text="{Binding Score}"  HorizontalAlignment="Center"/>
                                        <TextBlock Text="{Binding Marks}" TextDecorations="Underline" HorizontalAlignment="Center" />
                                    </StackPanel>
                                </Button>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </Grid>
            </ScrollViewer>
        </Grid>
        <Grid Grid.Column="2" Grid.Row="1" Margin="25">
            <Grid.RowDefinitions>
                <RowDefinition Height="4*"/>
                <RowDefinition Height="5*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="3*"/>
            </Grid.ColumnDefinitions>
            <StackPanel >
                <Image Height="220" Width="175" />
                <TextBlock Text="{Binding UserScore}"  HorizontalAlignment="Center"/>
                <TextBlock Text="{Binding MarksCount}" TextDecorations="Underline" HorizontalAlignment="Center" />
            </StackPanel>
            <StackPanel Grid.Column="2">
                <TextBlock  Text="Author: " HorizontalAlignment="Center"/>
            </StackPanel>
            <StackPanel Grid.ColumnSpan="2" Grid.Row="1">
                <TextBlock Text="Description: "/>
            </StackPanel>
        </Grid>
        
    </Grid>
</Window>
